apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-service
  labels:
    app: k8s-demo
spec:
  selector:
    app: k8s-demo
  ports:
    - Protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort